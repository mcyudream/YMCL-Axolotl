# YMCL 通用内容分发 + 自有更新平台（yda 插件 ymcl-content）
# 站点 origin 或带 /api 网关前缀的地址；客户端拼接 /api/plugins/ymcl-content/v1/**
# 开发环境示例：http://localhost:9002/api
# VITE_YMCL_CONTENT_URL=https://admin.example.com

# 可选：更新 API 独立 base（方便与内容站分离部署时修改）
# 可填 origin（https://host）或完整更新 API 前缀
# （https://host/api/plugins/ymcl-content/v1/update）
# 未配置时：优先从 VITE_YMCL_CONTENT_URL 推导；两者都空则回退 update.axlmc.org
# VITE_YMCL_UPDATE_URL=https://admin.example.com
